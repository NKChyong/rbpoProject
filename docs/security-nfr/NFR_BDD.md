# BDD Сценарии для NFR (Behavior-Driven Development)

> **Проект:** Reading List API
> **Версия:** 1.0
> **Формат:** Gherkin (Given/When/Then)

## Введение

Данный документ содержит BDD сценарии приемочного тестирования для ключевых нефункциональных требований. Сценарии написаны в формате Gherkin и могут быть автоматизированы с помощью инструментов типа pytest-bdd, behave или аналогичных.

---

## NFR-01: Хранение паролей (Bcrypt)

### Сценарий 1: Пароли хешируются с правильным cost factor

```gherkin
Feature: Безопасное хранение паролей
  Как система безопасности
  Я хочу чтобы пароли хешировались с использованием Bcrypt
  Для защиты от компрометации

  Scenario: Регистрация нового пользователя хеширует пароль
    Given новый пользователь с email "user@example.com" и паролем "SecurePass123!"
    When я отправляю POST запрос на /api/v1/auth/register
    Then пароль в базе данных должен быть хеширован
    And хеш пароля должен начинаться с "$2b$12$" (Bcrypt cost factor 12)
    And оригинальный пароль не должен быть восстановим из хеша

  Scenario: Верификация пароля использует безопасное сравнение
    Given существующий пользователь с хешированным паролем
    When я отправляю POST запрос на /api/v1/auth/login с правильным паролем
    Then время верификации должно быть ≥ 100ms (защита от timing attacks)
    And я должен получить JWT токены

  Scenario Outline: Слабые пароли отклоняются
    Given новый пользователь с паролем "<password>"
    When я отправляю POST запрос на /api/v1/auth/register
    Then я должен получить ошибку 400
    And сообщение должно содержать "Password does not meet requirements"

    Examples:
      | password   |
      | 123456     |
      | password   |
      | qwerty     |
      | abc        |
```

---

## NFR-02: JWT Токены (TTL)

### Сценарий 2: JWT токены имеют правильное время жизни

```gherkin
Feature: JWT токены с ограниченным TTL
  Как система аутентификации
  Я хочу чтобы JWT токены имели ограниченное время жизни
  Для минимизации рисков при компрометации

  Scenario: Access token истекает через 15 минут
    Given пользователь успешно аутентифицирован
    When я получаю access token
    Then поле "exp" в токене должно быть равно текущее_время + 900 секунд
    And через 16 минут токен должен быть невалиден
    And запрос с истекшим токеном должен вернуть 401 Unauthorized

  Scenario: Refresh token истекает через 7 дней
    Given пользователь успешно аутентифицирован
    When я получаю refresh token
    Then поле "exp" в токене должно быть равно текущее_время + 604800 секунд
    And через 8 дней токен должен быть невалиден

  Scenario: Обновление токена работает корректно
    Given у меня есть валидный refresh token
    And access token истек
    When я отправляю POST запрос на /api/v1/auth/refresh с refresh token
    Then я должен получить новый access token
    And новый access token должен быть валиден
    And refresh token может быть использован повторно (до истечения)

  Scenario: Использование скомпрометированного токена
    Given пользователь вышел из системы (logout)
    And refresh token добавлен в blacklist
    When я пытаюсь использовать этот refresh token
    Then я должен получить ошибку 401
    And сообщение должно содержать "Token has been revoked"
```

---

## NFR-03: Производительность аутентификации

### Сценарий 3: Производительность login endpoint

```gherkin
Feature: Производительность аутентификации
  Как система
  Я должна обеспечивать быстрый ответ на логин
  Для хорошего пользовательского опыта

  Scenario: p95 времени ответа при нормальной нагрузке
    Given система развернута на staging окружении
    And база данных содержит 10000 пользователей
    When я запускаю нагрузочный тест на POST /api/v1/auth/login
    And нагрузка составляет 50 RPS (requests per second)
    And тест длится 5 минут
    Then p95 времени ответа должен быть ≤ 300ms
    And p50 времени ответа должен быть ≤ 150ms
    And error rate должен быть < 0.1%

  Scenario: Производительность не деградирует под нагрузкой
    Given система под нагрузкой 50 RPS в течение 10 минут
    When я замеряю p95 в первую и последнюю минуту
    Then разница должна быть < 10%
    And CPU usage должен быть < 80%
    And память должна быть стабильна (нет memory leaks)

  @negative
  Scenario: Система устойчива к spike нагрузки
    Given система работает с нагрузкой 50 RPS
    When нагрузка резко возрастает до 200 RPS
    Then система не должна упасть
    And error rate может вырасти, но должен быть < 5%
    And система должна восстановиться за ≤ 60 секунд после снижения нагрузки
```

---

## NFR-04: Структурированные ошибки (RFC7807)

### Сценарий 4: Формат ошибок

```gherkin
Feature: Структурированные ошибки
  Как разработчик API клиента
  Я хочу получать ошибки в единообразном формате
  Для упрощения обработки ошибок

  Scenario Outline: Все типы ошибок возвращают структурированный формат
    When я отправляю <method> запрос на <endpoint> с <invalid_data>
    Then HTTP статус должен быть <status_code>
    And Content-Type должен быть "application/json"
    And ответ должен содержать поле "code"
    And ответ должен содержать поле "message"
    And ответ должен содержать поле "details"
    And заголовок "X-Request-ID" должен присутствовать

    Examples:
      | method | endpoint              | invalid_data        | status_code |
      | POST   | /api/v1/entries       | без токена          | 401         |
      | GET    | /api/v1/entries/99999 | несуществующий ID   | 404         |
      | POST   | /api/v1/entries       | невалидный JSON     | 400         |
      | POST   | /api/v1/auth/login    | неверный пароль     | 401         |

  Scenario: Ошибки не раскрывают чувствительную информацию
    When я отправляю POST запрос на /api/v1/auth/login с несуществующим email
    Then сообщение об ошибке должно быть generic: "Invalid credentials"
    And сообщение НЕ должно содержать "User not found"
    And сообщение НЕ должно содержать SQL запросы
    And сообщение НЕ должно содержать stack trace
```

---

## NFR-07: Rate Limiting

### Сценарий 5: Защита от flood атак

```gherkin
Feature: Rate Limiting
  Как система безопасности
  Я хочу ограничивать частоту запросов
  Для защиты от abuse и DoS атак

  Scenario: IP-based rate limiting для неаутентифицированных запросов
    Given я неаутентифицированный пользователь с IP "203.0.113.42"
    When я отправляю 100 запросов на /api/v1/auth/login в течение 1 минуты
    Then первые 100 запросов должны быть обработаны
    When я отправляю 101-й запрос
    Then я должен получить HTTP 429 Too Many Requests
    And заголовок "X-RateLimit-Limit" должен быть "100"
    And заголовок "X-RateLimit-Remaining" должен быть "0"
    And заголовок "Retry-After" должен указывать время в секундах

  Scenario: User-based rate limiting для аутентифицированных запросов
    Given я аутентифицированный пользователь
    When я отправляю 1000 запросов на /api/v1/entries в течение 1 минуты
    Then первые 1000 запросов должны быть обработаны
    When я отправляю 1001-й запрос
    Then я должен получить HTTP 429 Too Many Requests

  @negative
  Scenario: DDoS атака с нескольких IP
    Given атакующий использует 50 разных IP адресов
    When каждый IP отправляет 100 req/min (всего 5000 req/min)
    Then каждый IP должен быть ограничен независимо
    And легитимные пользователи не должны быть затронуты
```

---

## NFR-05: Уязвимости зависимостей

### Сценарий 6: Управление уязвимостями

```gherkin
Feature: Управление уязвимостями зависимостей
  Как команда безопасности
  Я хочу быстро устранять критические уязвимости
  Для минимизации рисков

  Scenario: CI pipeline обнаруживает новые уязвимости
    Given проект имеет зависимость с известной уязвимостью CVSS 9.5
    When запускается CI pipeline
    Then Semgrep должен обнаружить уязвимость
    And CI должен создать алерт
    And алерт должен содержать CVE номер и severity

  Scenario: Критические уязвимости устраняются в SLA
    Given обнаружена критическая уязвимость (CVSS ≥ 9.0)
    And прошло 7 дней с момента публикации
    When я проверяю проект
    Then уязвимость должна быть устранена (патч/обновление)
    OR должен существовать documented mitigation plan
    AND risk acceptance от security team

  @negative
  Scenario: Деплой блокируется при критических уязвимостях
    Given проект имеет неустраненную критическую уязвимость > 7 дней
    When я пытаюсь задеплоить в production
    Then деплой должен быть заблокирован
    And должно быть выведено сообщение с деталями уязвимости
```

---

## NFR-09: SQL Injection защита

### Сценарий 7: Защита от SQL Injection

```gherkin
Feature: Защита от SQL Injection
  Как система безопасности
  Я должна предотвращать SQL Injection атаки
  Для защиты данных

  Scenario Outline: SQL Injection попытки блокируются
    Given я аутентифицированный пользователь
    When я отправляю GET запрос на /api/v1/entries?search=<malicious_input>
    Then запрос должен быть безопасно обработан
    And я НЕ должен получить данные из других таблиц
    And в логах НЕ должно быть SQL ошибок

    Examples:
      | malicious_input                          |
      | ' OR '1'='1                              |
      | '; DROP TABLE entries; --                |
      | 1' UNION SELECT * FROM users--           |
      | admin'--                                  |

  Scenario: Все DB запросы используют ORM
    Given я запускаю SAST сканирование (Semgrep)
    When анализатор проверяет код
    Then НЕ должно быть найдено raw SQL запросов с конкатенацией строк
    AND все взаимодействия с БД должны идти через SQLAlchemy ORM
```

---

## Негативные сценарии (Edge Cases)

### Сценарий 8: Граничные случаи и отказоустойчивость

```gherkin
Feature: Отказоустойчивость системы
  Как надежная система
  Я должна корректно обрабатывать граничные случаи
  Для стабильной работы

  @negative
  Scenario: База данных недоступна
    Given PostgreSQL база данных недоступна
    When пользователь пытается залогиниться
    Then API должен вернуть 503 Service Unavailable
    And сообщение должно быть generic: "Service temporarily unavailable"
    And в логах должна быть запись с деталями (для debugging)
    And healthcheck endpoint должен показывать "unhealthy"

  @negative
  Scenario: Очень большой payload
    Given я аутентифицированный пользователь
    When я отправляю POST /api/v1/entries с телом размером 10 MB
    Then запрос должен быть отклонен с 413 Payload Too Large
    And система НЕ должна упасть (no crash)
    And память НЕ должна утечь

  @negative
  Scenario: Одновременная модификация ресурса (Race Condition)
    Given существует entry с ID 123
    When два пользователя одновременно отправляют PATCH /api/v1/entries/123
    Then один запрос должен быть успешен (200 OK)
    And второй запрос должен получить 409 Conflict или быть применен корректно
    And данные НЕ должны быть повреждены (data integrity)

  @negative
  Scenario: Истощение пула соединений к БД
    Given система под высокой нагрузкой
    And все соединения к БД заняты
    When приходит новый запрос
    Then запрос должен ожидать свободное соединение (с timeout)
    OR получить 503 Service Unavailable после timeout
    AND система должна восстановиться автоматически
```

---

## Автоматизация

### Инструменты для автоматизации BDD сценариев

1. **pytest-bdd** - для Python/pytest
2. **behave** - для Python (Cucumber-style)
3. **Locust / k6** - для нагрузочного тестирования (NFR-03)
4. **Semgrep** - для SAST проверок (NFR-05, NFR-09)

### Пример pytest-bdd

```python
# tests/bdd/test_nfr_01_password_hashing.py

from pytest_bdd import scenarios, given, when, then
import bcrypt

scenarios('../../docs/security-nfr/NFR_BDD.md')

@given('новый пользователь с email "user@example.com" и паролем "SecurePass123!"')
def new_user(context):
    context.email = "user@example.com"
    context.password = "SecurePass123!"

@when('я отправляю POST запрос на /api/v1/auth/register')
def register_user(context, client):
    response = client.post('/api/v1/auth/register', json={
        "email": context.email,
        "username": "testuser",
        "password": context.password
    })
    context.response = response

@then('пароль в базе данных должен быть хеширован')
def check_password_hashed(context, db):
    user = db.query(User).filter_by(email=context.email).first()
    assert user.password_hash != context.password
    assert user.password_hash.startswith('$2b$')

@then('хеш пароля должен начинаться с "$2b$12$" (Bcrypt cost factor 12)')
def check_bcrypt_cost_factor(context, db):
    user = db.query(User).filter_by(email=context.email).first()
    assert user.password_hash.startswith('$2b$12$')
```

---

## Метрики успеха

| NFR ID | Критерий                                    | Целевое значение | Статус |
|--------|---------------------------------------------|------------------|--------|
| NFR-01 | 100% паролей хешировано с Bcrypt CF 12      | 100%             | ✅      |
| NFR-02 | Access token TTL = 15 мин                   | 900s             | ✅      |
| NFR-03 | p95 login < 300ms @ 50 RPS                  | ≤ 300ms          | 🔄      |
| NFR-04 | 100% errors в RFC7807 формате               | 100%             | ✅      |
| NFR-05 | Критические уязвимости устранены за ≤ 7 дней | ≤ 7 дней       | ✅      |
| NFR-07 | Rate limiting 100/1000 req/min              | Configured       | 🔄      |
| NFR-09 | 0 SQL injection уязвимостей                 | 0                | ✅      |

---

## Ссылки

- [NFR документ](./NFR.md)
- [Traceability Matrix](./NFR_TRACEABILITY.md)
- [pytest-bdd documentation](https://pytest-bdd.readthedocs.io/)
- [Gherkin Reference](https://cucumber.io/docs/gherkin/reference/)

## История изменений

| Дата       | Версия | Изменения                    | Автор    |
|------------|--------|------------------------------|----------|
| 2025-10-13 | 1.0    | Первая версия BDD сценариев  | NKChyong |
